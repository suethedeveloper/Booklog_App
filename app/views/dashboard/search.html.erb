<div class="container">
  <div class="row search-result">
    <% @readers.each do |reader| %>
    <div class="col-md-10 col-xs-offset-1 well member">
      <%= reader.full_name %>
    </div>
    <% end %>
  </div>
  <%= will_paginate @readers, renderer: BootstrapPagination::Rails %>
</div>